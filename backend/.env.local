# Environment Configuration

# GCP Firestore Configuration
# For local development, set GOOGLE_APPLICATION_CREDENTIALS to your service account key
# In GCP (Cloud Run, App Engine, etc.), Application Default Credentials are used automatically
GCP_PROJECT_ID=newswatch-local
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json  # Set this in your shell, not here

# Firestore Emulator (for local development without GCP)
# Uncomment to use local emulator instead of GCP Firestore
FIRESTORE_EMULATOR_HOST=localhost:8080

# AI Configuration
# AI Configuration
# Set AI_PROVIDER to 'gemini' or 'openai' (openai works with LM Studio too)
AI_PROVIDER=openai
# GEMINI_API_KEY=AIzaSyBprQO90Dle-4A_ApmjucBeGxjfZeP6oTg

# OpenAI / LM Studio Configuration
# OPENAI_BASE_URL=http://localhost:1234/v1
OPENAI_API_KEY=
AI_MODEL=gpt-5.1-2025-11-13

# Email Service (optional - will log to console if not set)
SENDGRID_API_KEY=
EMAIL_FROM=newsletter@newswatch.local
ADMIN_EMAIL=lairdapopkin@hotmail.com

# Server Configuration
PORT=3000
NODE_ENV=development

# Newsletter Schedule
NEWSLETTER_SEND_TIME=06:00
NEWSLETTER_TIMEZONE=America/New_York

GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# Feedback Loop Settings (set to false for production)
SHOW_GUIDANCE_IN_NEWSLETTER=true
AUTO_SEND_ON_FEEDBACK=true
ENABLE_TEST_TRIGGER=true
