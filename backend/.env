# Environment Configuration

# Database
DATABASE_URL=postgresql://postgres:topsekret@localhost:5432/newswatch
# Or use: postgresql://user:password@localhost:5432/newswatch

# OpenAI API (optional - will use mock responses if not set)
OPENAI_API_KEY=

# Email Service (optional - will log to console if not set)
SENDGRID_API_KEY=
EMAIL_FROM=newsletter@newswatch.local
ADMIN_EMAIL=admin@newswatch.local

# Server Configuration
PORT=3000
NODE_ENV=development

# Newsletter Schedule
NEWSLETTER_SEND_TIME=06:00
NEWSLETTER_TIMEZONE=America/New_York
