# Environment Configuration

# Database
# Using local socket in /tmp since we started postgres with -k /tmp
DATABASE_URL=postgresql://laird@localhost:5432/newswatch?host=/tmp

# AI Configuration
AI_PROVIDER=gemini
GEMINI_API_KEY=AIzaSyBprQO90Dle-4A_ApmjucBeGxjfZeP6oTg

# LM Studio (Optional Fallback)
OPENAI_BASE_URL=http://localhost:1234/v1
OPENAI_API_KEY=lm-studio
AI_MODEL=local-model

# Email Service (optional - will log to console if not set)
SENDGRID_API_KEY=
EMAIL_FROM=newsletter@newswatch.local
ADMIN_EMAIL=laird@popk.in

# Server Configuration
PORT=3000
NODE_ENV=development

# Newsletter Schedule
NEWSLETTER_SEND_TIME=06:00
NEWSLETTER_TIMEZONE=America/New_York
