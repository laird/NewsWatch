# Environment Configuration

# Google Cloud / Firestore
# Set your GCP project ID
GCP_PROJECT_ID=your-project-id

# For local development, set path to service account JSON key file
# In production on GCP (Cloud Run), credentials are auto-detected
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# For local development with Firestore emulator (optional)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# AI Provider Configuration
# Options: 'auto', 'gemini', 'openai'
AI_PROVIDER=auto

# Google Gemini API (recommended for GCP)
GEMINI_API_KEY=

# OpenAI API (optional alternative)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4

# Email Service (optional - will log to console if not set)
SENDGRID_API_KEY=
EMAIL_FROM=newsletter@newswatch.local
ADMIN_EMAIL=admin@newswatch.local

# Server Configuration
PORT=3000
NODE_ENV=development

# Newsletter Schedule
NEWSLETTER_SEND_TIME=06:00
NEWSLETTER_TIMEZONE=America/New_York
